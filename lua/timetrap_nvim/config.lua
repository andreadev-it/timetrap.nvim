local configs = {
    display = {
        -- win_type - options: "float", "horizontal", "vertical"
        win_type = "float",
        -- border - options: all those supported by nui.popup (basically those in the help for nvim_open_win)
        border = "rounded"
    },
    -- prompts - options: "input", "float"
    prompts = "float"
}

return configs
